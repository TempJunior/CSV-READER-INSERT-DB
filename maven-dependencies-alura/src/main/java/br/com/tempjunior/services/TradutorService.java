package br.com.tempjunior.services;

import br.com.tempjunior.model.Product;
import space.dynomake.libretranslate.Language;
import space.dynomake.libretranslate.Translator;

/*public class TradutorService {
    public void traduz(Product product) {
        String novoNome = Translator.translate(Language.ENGLISH, Language.PORTUGUESE, product.getName());
        String novaDescricao = Translator.translate(Language.ENGLISH, Language.PORTUGUESE, product.getDescription());
        String novaCategoria = Translator.translate(Language.ENGLISH, Language.PORTUGUESE, product.getCategory());

        product.setName(novoNome);
        product.setDescription(novaDescricao);
        product.setCategory(novaCategoria);
    }
}*/
